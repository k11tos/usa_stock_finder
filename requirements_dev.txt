-r requirements.txt

# Code Quality
pylint==3.2.1

# Pre-commit hooks (managed via .pre-commit-config.yaml)
# Note: pre-commit installs its own dependencies automatically

# Testing
pytest>=8.0.0

# Type Checking (optional - currently disabled in .pre-commit-config.yaml)
# mypy>=1.8.0
# types-requests>=2.31.0
# types-PyYAML>=6.0.0
# types-python-dateutil>=2.8.0

# Security
# bandit>=1.7.5  # Currently disabled in .pre-commit-config.yaml
safety>=2.3.5  # Dependency vulnerability scanning
